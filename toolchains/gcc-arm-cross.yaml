toolchain:
  name: "gcc-arm-cross"
  description: "GCC ARM cross-compiler for embedded systems"
  
  target:
    platform: linux
    architecture: arm
  
  host:
    platform: linux
    architecture: x86_64
  
  execution:
    type: native
  
  tools:
    # C compilation
    c_compile:
      action: compile
      command: "arm-linux-gnueabihf-gcc {dep_flags} {flags} {defines} {pic} {includes} -c {input} -o {output}"
      input_extensions: [".c"]
      output_extension: ".o"
      flags:
        common: ["-Wall", "-Wextra"]
        debug: ["-g", "-O0"]
        release: ["-O3", "-DNDEBUG"]
      supports:
        defines: true
        define_flag: "-D"
        includes: true
        include_flag: "-I"
        pic: true
        pic_flag: "-fPIC"
        dependencies: "-MMD -MP -MF {dep_file}"
    
    # C++ compilation
    cpp_compile:
      action: compile
      command: "arm-linux-gnueabihf-g++ {dep_flags} -std={std} {flags} {defines} {pic} {includes} -c {input} -o {output}"
      input_extensions: [".cpp", ".cc", ".cxx", ".C"]
      output_extension: ".o"
      flags:
        common: ["-Wall", "-Wextra"]
        debug: ["-g", "-O0"]
        release: ["-O3", "-DNDEBUG"]
      supports:
        defines: true
        define_flag: "-D"
        includes: true
        include_flag: "-I"
        pic: true
        pic_flag: "-fPIC"
        dependencies: "-MMD -MP -MF {dep_file}"
        std: true
    
    # Shared library linking
    link_shared_library:
      action: link
      command: "arm-linux-gnueabihf-g++ -shared {flags} {objects} {lib_dirs} {libs} -o {output}"
      input_extensions: [".o"]
      output_extension: ".so"
      output_pattern: "lib{name}.so"
      flags:
        common: []
        debug: []
        release: ["-s"]
      supports:
        output_type: "shared_library"
        lib_dirs: true
        lib_dir_flag: "-L"
        libs: true
        lib_flag: "-l"
    
    # Static library archiving
    link_static_library:
      action: link
      command: "arm-linux-gnueabihf-ar rcs {output} {objects}"
      input_extensions: [".o"]
      output_extension: ".a"
      output_pattern: "lib{name}.a"
      flags:
        common: []
        debug: []
        release: []
      supports:
        output_type: "static_library"
        lib_dirs: false
        libs: false
    
    # Executable linking
    link_executable:
      action: link
      command: "arm-linux-gnueabihf-g++ {flags} {objects} {lib_dirs} {libs} -o {output}"
      input_extensions: [".o"]
      output_extension: ""
      output_pattern: "{name}"
      flags:
        common: []
        debug: []
        release: ["-s"]
      supports:
        output_type: "executable"
        lib_dirs: true
        lib_dir_flag: "-L"
        libs: true
        lib_flag: "-l"
